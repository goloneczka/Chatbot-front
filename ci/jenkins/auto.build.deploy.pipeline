pipeline {
    agent any
    stages {
        stage('Build and deploy') {
            steps {
                build job: 'pip2020-frontend-build-deploy/develop', wait: true, parameters: [
                        booleanParam(name: 'MODIFICATION', value: true)
                ]
            }
        }
    }
}
